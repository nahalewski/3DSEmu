diff --git a/mobile/android/love/src/jni/love/src/modules/system/System.cpp b/mobile/android/love/src/jni/love/src/modules/system/System.cpp
index 3af28e9b..baa9d9d6 100644
--- a/mobile/android/love/src/jni/love/src/modules/system/System.cpp
+++ b/mobile/android/love/src/jni/love/src/modules/system/System.cpp
@@ -197,6 +197,9 @@ bool System::pickFile(const char *kind, const char *destination) const
 				? destination : "picked_required_import.bin";
 		else if (strcmp(kind, "rom") == 0)
 			dest = "picked_rom.gb";
+		// a picture for the foldable layer's cover sticker (fold3ds)
+		else if (strcmp(kind, "image") == 0)
+			dest = "picked_image.img";
 		// Unknown kinds used to fall through to the ROM destination. Refuse them
 		// so a newer Lua caller cannot silently route an unrelated file as a ROM.
 		else
@@ -212,7 +215,7 @@ bool System::pickFile(const char *kind, const char *destination) const
 const char *System::pickFileKinds() const
 {
 #ifdef LOVE_ANDROID
-	return "rom,mod,sav,required_import";
+	return "rom,mod,sav,required_import,image";
 #else
 	return "";
 #endif
